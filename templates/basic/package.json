{
  "name": "basic",
  "type": "module",
  "private": true,
  "module": "index.ts",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "lint": "bunx --bun eslint",
    "format": "bunx --bun eslint --fix",
    "typecheck": "tsc --noEmit",
    "typecheck:tsgo": "tsgo --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@acfatah/eslint-preset": "^1.4.1",
    "@types/bun": "^1.3.5",
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  }
}
